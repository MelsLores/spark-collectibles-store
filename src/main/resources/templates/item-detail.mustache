<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}} - Collectibles Store</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/styles.css">
</head>
                    <div class="card-body p-4">
                        <div class="row">
                            <div class="col-md-6 text-center mb-3">
                                <img src="/products/{{image}}" alt="{{name}}" class="img-fluid rounded product-detail-image" />
                            </div>
                            <div class="col-md-6">
                                <!-- Price Badge -->
                                <div class="mb-2">
                                    <h3 class="mb-1">{{name}}</h3>
                                    <small class="text-muted">{{artist}}</small>
                                </div>
                                    <div class="mb-4 text-center text-md-start">
                                        <span class="badge fs-2 px-4 py-3 price-badge">
                                            <i class="bi bi-currency-dollar"></i> {{price}}
                                        </span>
                                </div>

                                <!-- Description -->
                                <div class="mb-4">
                                    <h5 class="text-primary mb-3">
                                        <i class="bi bi-info-circle"></i> Description
                                    </h5>
                                    <p class="lead text-muted">{{description}}</p>
                                </div>

                                <!-- Item Details -->
                                <div class="row mb-4">
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center mb-3">
                                            <i class="bi bi-hash text-primary fs-4 me-2"></i>
                                            <div>
                                                <small class="text-muted d-block">Item ID</small>
                                                <strong>{{id}}</strong>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center mb-3">
                                            <i class="bi bi-check-circle text-success fs-4 me-2"></i>
                                            <div>
                                                <small class="text-muted d-block">Status</small>
                                                <strong>Available</strong>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                        <!-- Offers for this item -->
                                        <div class="mt-4">
                                            <h5 class="text-primary mb-3">
                                                <i class="bi bi-hand-thumbs-up"></i> Offers for this item
                                            </h5>
                                            {{#hasOffers}}
                                            <div class="table-responsive">
                                                <table class="table table-striped">
                                                    <thead>
                                                        <tr>
                                                            <th>Offer ID</th>
                                                            <th>Buyer</th>
                                                            <th>Amount</th>
                                                            <th></th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        {{#offers}}
                                                        <tr>
                                                            <td>{{id}}</td>
                                                            <td>
                                                                {{name}}<br>
                                                                <small class="text-muted">{{email}}</small>
                                                            </td>
                                                            <td>{{amount}}</td>
                                                            <td>
                                                                <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#offer-{{id}}" aria-expanded="false">
                                                                    View
                                                                </button>
                                                            </td>
                                                        </tr>
                                                        <tr class="collapse" id="offer-{{id}}">
                                                            <td colspan="4">
                                                                <div class="p-2">
                                                                    <strong>Offer Details</strong>
                                                                    <p class="mb-1">Offer ID: {{id}}</p>
                                                                    <p class="mb-1">Buyer: {{name}} — <a href="mailto:{{email}}">{{email}}</a></p>
                                                                    <p class="mb-0">Amount: {{amount}}</p>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                        {{/offers}}
                                                    </tbody>
                                                </table>
                                            </div>
                                            {{/hasOffers}}
                                            {{^hasOffers}}
                                            <p class="text-muted">No offers yet for this item.</p>
                                            {{/hasOffers}}
                                        </div>

                                <!-- Action Buttons -->
                                <div class="d-grid gap-2">
                                        <a href="/offers/new?itemId={{id}}" class="btn btn-danger btn-lg">
                                            <i class="bi bi-tags"></i> Make an Offer
                                        </a>
                                    <a href="/items" class="btn btn-outline-primary">
                                        <i class="bi bi-arrow-left"></i> Back to Items List
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-body p-4">
                        <!-- Price Badge -->
                        <div class="mb-4 text-center">
                            <span class="badge bg-success fs-2 px-4 py-3">
                                <i class="bi bi-currency-dollar"></i> {{price}}
                            </span>
                        </div>

                        <!-- Description -->
                        <div class="mb-4">
                            <h5 class="text-primary mb-3">
                                <i class="bi bi-info-circle"></i> Description
                            </h5>
                            <p class="lead text-muted">{{description}}</p>
                        </div>

                        <!-- Item Details -->
                        <div class="row mb-4">
                            <div class="col-md-6">
                                <div class="d-flex align-items-center mb-3">
                                    <i class="bi bi-hash text-primary fs-4 me-2"></i>
                                    <div>
                                        <small class="text-muted d-block">Item ID</small>
                                        <strong>{{id}}</strong>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="d-flex align-items-center mb-3">
                                    <i class="bi bi-check-circle text-success fs-4 me-2"></i>
                                    <div>
                                        <small class="text-muted d-block">Status</small>
                                        <strong>Available</strong>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="d-grid gap-2">
                            <a href="/offers/new?itemId={{id}}" class="btn btn-success btn-lg">
                                <i class="bi bi-tags"></i> Make an Offer
                            </a>
                            <a href="/items" class="btn btn-outline-primary">
                                <i class="bi bi-arrow-left"></i> Back to Items List
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Interest Info -->
                <div class="alert alert-info mt-4" role="alert">
                    <i class="bi bi-info-circle-fill"></i>
                    <strong>Interested in this item?</strong> Make an offer and we'll contact you!
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="brand-bg text-white mt-5 py-4">
        <div class="container text-center">
            <p class="mb-0">
                <i class="bi bi-c-circle"></i> 2025 Collectibles Store - Digital NAO
            </p>
            <small class="text-muted">Sprint 2 - Enhanced UI with Bootstrap 5</small>
        </div>
    </footer>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
